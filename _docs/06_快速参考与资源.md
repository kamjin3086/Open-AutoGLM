# Open-AutoGLM 快速参考与资源

本文档提供快速查询和学习资源汇总。

---

## 🚀 快速开始

### 环境搭建（10分钟）

```bash
# 1. 安装 Python 依赖
pip install -r requirements.txt
pip install -e .

# 2. 检查 ADB
adb version

# 3. 连接设备
adb devices

# 4. 启动模型服务 (示例)
python3 -m vllm.entrypoints.openai.api_server \
  --served-model-name autoglm-phone-9b \
  --model zai-org/AutoGLM-Phone-9B \
  --port 8000

# 5. 运行第一个任务
python main.py --base-url http://localhost:8000/v1 "打开微信"
```

### 第一个 Python 脚本

```python
from phone_agent import PhoneAgent
from phone_agent.model import ModelConfig

# 配置
config = ModelConfig(
    base_url="http://localhost:8000/v1",
    model_name="autoglm-phone-9b"
)

# 创建 Agent
agent = PhoneAgent(model_config=config)

# 执行任务
result = agent.run("打开小红书搜索美食攻略")
print(result)
```

---

## 📚 核心概念速查

### 坐标系统

| 类型 | 范围 | 用途 |
|------|------|------|
| 相对坐标 | 0-1000 | 模型输出 |
| 绝对坐标 | 0-screen_size | ADB 命令 |

**转换公式**:
```python
x_pixel = x_relative / 1000 * screen_width
y_pixel = y_relative / 1000 * screen_height
```

### 动作类型

| 动作 | 格式 | 示例 |
|------|------|------|
| Launch | `do(action="Launch", app="应用名")` | `do(action="Launch", app="微信")` |
| Tap | `do(action="Tap", element=[x,y])` | `do(action="Tap", element=[500,200])` |
| Type | `do(action="Type", text="文本")` | `do(action="Type", text="搜索内容")` |
| Swipe | `do(action="Swipe", start=[x1,y1], end=[x2,y2])` | `do(action="Swipe", start=[500,800], end=[500,200])` |
| Back | `do(action="Back")` | `do(action="Back")` |
| Home | `do(action="Home")` | `do(action="Home")` |
| Finish | `finish(message="消息")` | `finish(message="任务完成")` |

### 消息格式

```python
# System Message
{
    "role": "system",
    "content": "系统提示词..."
}

# User Message (带图像)
{
    "role": "user",
    "content": [
        {"type": "image_url", "image_url": {"url": "data:image/png;base64,xxx"}},
        {"type": "text", "text": "任务描述"}
    ]
}

# Assistant Message
{
    "role": "assistant",
    "content": "<think>思考</think><answer>动作</answer>"
}
```

---

## 🛠️ 常用命令

### ADB 命令

```bash
# 设备管理
adb devices                              # 列出设备
adb connect 192.168.1.100:5555          # 连接远程设备
adb disconnect 192.168.1.100:5555       # 断开设备
adb kill-server && adb start-server     # 重启 ADB 服务

# 文件传输
adb push local.txt /sdcard/             # 上传文件
adb pull /sdcard/remote.txt .           # 下载文件

# 应用管理
adb shell pm list packages              # 列出所有应用
adb shell pm dump <package>             # 查看应用信息
adb install app.apk                     # 安装应用
adb uninstall <package>                 # 卸载应用

# 输入操作
adb shell input tap 500 1000            # 点击
adb shell input swipe 500 1000 500 500  # 滑动
adb shell input text "Hello"            # 输入文本
adb shell input keyevent 4              # 按键 (4=返回)

# 屏幕相关
adb shell screencap -p /sdcard/screen.png  # 截图
adb shell wm size                       # 查看分辨率
adb shell wm density                    # 查看 DPI

# 输入法
adb shell ime list -s                   # 列出输入法
adb shell ime set com.android.adbkeyboard/.AdbIME  # 切换输入法

# 查看当前应用
adb shell dumpsys window | grep mCurrentFocus
```

### Python API

```python
# Agent 相关
agent = PhoneAgent()
agent.run("任务描述")                    # 执行完整任务
agent.step("任务描述")                   # 执行单步
agent.reset()                           # 重置状态
agent.step_count                        # 当前步数
agent.context                           # 对话历史

# 配置相关
from phone_agent.model import ModelConfig
from phone_agent.agent import AgentConfig

model_config = ModelConfig(
    base_url="http://localhost:8000/v1",
    model_name="autoglm-phone-9b",
    temperature=0.0,
    max_tokens=3000
)

agent_config = AgentConfig(
    max_steps=100,
    device_id=None,
    verbose=True
)

# ADB 相关
from phone_agent.adb import *

get_screenshot(device_id)               # 截图
get_current_app(device_id)              # 当前应用
tap(x, y, device_id)                    # 点击
swipe(x1, y1, x2, y2, device_id)       # 滑动
type_text(text, device_id)              # 输入
back(device_id)                         # 返回
home(device_id)                         # 主屏幕
launch_app(app_name, device_id)         # 启动应用

# 连接管理
from phone_agent.adb import ADBConnection, list_devices

conn = ADBConnection()
conn.connect("192.168.1.100:5555")      # 连接
conn.disconnect("192.168.1.100:5555")   # 断开
conn.enable_tcpip(5555)                 # 启用 TCP/IP
conn.get_device_ip()                    # 获取设备 IP

devices = list_devices()                # 列出设备
```

---

## 🐛 常见问题排查

### 问题 1: 设备未找到

**症状**:
```
adb devices
List of devices attached
(空)
```

**解决方案**:
```bash
# 1. 重启 ADB
adb kill-server
adb start-server

# 2. 检查 USB 调试是否启用
# 设置 → 开发者选项 → USB 调试

# 3. 检查 USB 线是否支持数据传输

# 4. 重新授权
adb devices  # 手机上会弹出授权对话框
```

---

### 问题 2: 文本输入失败

**症状**: Type 动作执行后没有文本输入

**解决方案**:
```bash
# 1. 检查 ADB Keyboard 是否安装
adb shell ime list -s | grep adbkeyboard

# 2. 如果未安装，安装 ADB Keyboard
adb install ADBKeyboard.apk

# 3. 启用 ADB Keyboard
# 设置 → 系统 → 语言和输入法 → 虚拟键盘 → 启用 ADB Keyboard

# 4. 测试输入
adb shell am broadcast -a ADB_INPUT_TEXT --es msg "测试"
```

---

### 问题 3: 模型 API 连接失败

**症状**:
```
Model error: Connection refused
```

**解决方案**:
```bash
# 1. 检查模型服务是否启动
curl http://localhost:8000/v1/models

# 2. 检查端口是否正确
netstat -an | grep 8000

# 3. 检查防火墙设置

# 4. 如果在远程服务器，使用正确的 IP
python main.py --base-url http://192.168.1.100:8000/v1
```

---

### 问题 4: 截图黑屏

**症状**: 截图内容全黑

**原因**: 
1. 敏感页面 (支付、密码)
2. 应用阻止截图
3. DRM 保护的内容

**解决方案**:
- 检查 `screenshot.is_sensitive` 标记
- 让 AI 执行 `Take_over` 动作
- 手动完成操作后继续

---

### 问题 5: 任务陷入循环

**症状**: AI 重复执行相同的动作

**解决方案**:
1. **增强提示词**:
```python
# 在 prompts.py 中添加规则
"如果连续 3 次执行相同操作仍无进展，尝试其他策略或返回上一步。"
```

2. **降低 max_steps**:
```python
agent_config = AgentConfig(max_steps=50)  # 从 100 降到 50
```

3. **增加调试信息**: 设置 `verbose=True`

---

## 📖 学习资源

### 官方资源

- **项目 GitHub**: [Open-AutoGLM](https://github.com/THUDM/AutoGLM-Phone)
- **AutoGLM 论文**: [arXiv:2411.00820](https://arxiv.org/abs/2411.00820)
- **GLM-V 项目**: [GLM-V GitHub](https://github.com/zai-org/GLM-V)
- **模型下载**: [Hugging Face](https://huggingface.co/zai-org/AutoGLM-Phone-9B)

### 相关技术

**多模态模型**:
- GPT-4V: [OpenAI 官网](https://openai.com/gpt-4)
- Gemini: [Google AI](https://deepmind.google/technologies/gemini/)
- Claude 3: [Anthropic](https://www.anthropic.com/claude)
- Qwen-VL: [Qwen GitHub](https://github.com/QwenLM/Qwen-VL)

**Agent 系统**:
- AppAgent: [GitHub](https://github.com/mnotgod96/AppAgent)
- WebAgent: [GitHub](https://github.com/web-arena-x/webarena)
- Mobile-Env: [GitHub](https://github.com/X-LANCE/Mobile-Env)

**Android 自动化**:
- UIAutomator: [Android 官方文档](https://developer.android.com/training/testing/ui-automator)
- Appium: [官网](http://appium.io/)
- ADB 文档: [Android Debug Bridge](https://developer.android.com/studio/command-line/adb)

**Prompt Engineering**:
- OpenAI Prompt 指南: [Best Practices](https://platform.openai.com/docs/guides/prompt-engineering)
- Anthropic Prompt 库: [Prompt Library](https://docs.anthropic.com/claude/prompt-library)

### 推荐论文

1. **AutoGLM** (本项目基础):
   ```
   Liu et al., "AutoGLM: Autonomous Foundation Agents for GUIs", 2024
   arXiv:2411.00820
   ```

2. **Chain-of-Thought**:
   ```
   Wei et al., "Chain-of-Thought Prompting Elicits Reasoning in Large Language Models", 2022
   NeurIPS 2022
   ```

3. **Vision-Language Models**:
   ```
   Alayrac et al., "Flamingo: a Visual Language Model for Few-Shot Learning", 2022
   NeurIPS 2022
   ```

4. **Mobile Agent**:
   ```
   Zhang et al., "You Only Look at Screens: Multimodal Chain-of-Action Agents", 2024
   arXiv:2309.11436
   ```

### 视频教程

- **AutoGLM 项目介绍**: [Bilibili](https://www.bilibili.com/)
- **ADB 使用教程**: YouTube 搜索 "ADB Tutorial"
- **多模态模型原理**: 李宏毅《机器学习》课程

### 社区

- **项目微信群**: 见 `resources/WECHAT.md`
- **GitHub Issues**: 提问和讨论
- **知乎专栏**: 搜索"AutoGLM"

---

## 🎯 实践项目建议

### 初级项目

1. **个人助手**:
   - 定时发送消息
   - 查询天气、新闻
   - 记录日程

2. **自动化测试**:
   - 测试应用的登录流程
   - 检查 UI 是否正常显示
   - 压力测试

3. **信息收集**:
   - 自动截图并 OCR 提取文本
   - 爬取应用内容
   - 生成报告

### 中级项目

1. **智能客服**:
   - 自动回复消息
   - 处理客户咨询
   - 转接人工

2. **游戏辅助**:
   - 自动完成任务
   - 资源采集
   - 副本挂机

3. **内容创作**:
   - 自动发布动态
   - 批量点赞评论
   - 数据分析

### 高级项目

1. **跨应用工作流**:
   - 从微信读取订单 → 淘宝下单 → 支付宝支付
   - 自动化复杂业务流程

2. **多模态搜索**:
   - 拍照搜索商品
   - 截图提问
   - 视频内容理解

3. **智能代理系统**:
   - 学习用户习惯
   - 主动推荐内容
   - 预测性操作

---

## 📊 性能基准

### 硬件要求

| 组件 | 最低配置 | 推荐配置 |
|------|----------|----------|
| CPU | 4 核 | 8 核+ |
| 内存 | 8GB | 16GB+ |
| GPU | - | RTX 3090 / A100 |
| 存储 | 50GB | 200GB+ |
| 网络 | 10Mbps | 100Mbps+ |

### 性能指标

**延迟**:
- 单步执行: 2-5 秒
- 其中模型推理: 1-3 秒
- 截图 + 执行: 1-2 秒

**准确率** (10 个常见任务):
- 简单任务 (如启动应用): ~95%
- 中等任务 (如搜索商品): ~80%
- 复杂任务 (如多步购物): ~60%

**成本** (使用 vLLM):
- 单次推理: ~0.01-0.02 元 (云服务)
- 10 步任务: ~0.1-0.2 元
- 月度运行 100 任务: ~10-20 元

---

## 🔧 开发工具

### IDE 推荐

- **VS Code**: 最佳 Python 开发体验
  - 插件: Python, Pylance, Black Formatter
- **PyCharm**: 强大的调试功能
- **Jupyter Notebook**: 交互式实验

### 调试工具

```python
# 1. IPython 调试
from IPython import embed
embed()  # 在此处进入交互式 shell

# 2. pdb 调试
import pdb
pdb.set_trace()  # 断点

# 3. 日志记录
import logging
logging.basicConfig(level=logging.DEBUG)
```

### 代码格式化

```bash
# Black (代码格式化)
pip install black
black phone_agent/

# isort (导入排序)
pip install isort
isort phone_agent/

# mypy (类型检查)
pip install mypy
mypy phone_agent/

# flake8 (代码检查)
pip install flake8
flake8 phone_agent/
```

---

## 📝 代码片段

### 自定义回调

```python
# GUI 确认对话框
from tkinter import messagebox

def gui_confirmation(message: str) -> bool:
    return messagebox.askyesno("确认", message)

# 日志记录
def logging_confirmation(message: str) -> bool:
    logging.info(f"Confirmation requested: {message}")
    return True  # 自动确认

# API 回调
def api_confirmation(message: str) -> bool:
    response = requests.post("https://api.example.com/confirm", json={
        "message": message
    })
    return response.json()["confirmed"]
```

### 批量任务

```python
tasks = [
    "打开微信查看消息",
    "打开淘宝搜索无线耳机",
    "打开抖音刷视频"
]

agent = PhoneAgent()

for i, task in enumerate(tasks, 1):
    print(f"\n{'='*50}")
    print(f"任务 {i}/{len(tasks)}: {task}")
    print('='*50)
    
    result = agent.run(task)
    print(f"结果: {result}")
    
    agent.reset()
    time.sleep(5)  # 任务间隔
```

### 错误重试

```python
def run_with_retry(agent, task, max_retries=3):
    """带重试的任务执行"""
    for attempt in range(max_retries):
        try:
            result = agent.run(task)
            if "Max steps reached" not in result:
                return result
        except Exception as e:
            print(f"尝试 {attempt + 1} 失败: {e}")
        
        if attempt < max_retries - 1:
            print("重试中...")
            agent.reset()
            time.sleep(5)
    
    return "所有重试均失败"
```

---

## 🎓 学习检查表

完成学习后，你应该能够：

### 基础能力
- [ ] 搭建开发环境
- [ ] 运行基本任务
- [ ] 理解项目架构
- [ ] 阅读核心代码

### 进阶能力
- [ ] 修改提示词
- [ ] 添加新动作
- [ ] 集成 UI 元素
- [ ] 优化性能

### 高级能力
- [ ] 设计新功能
- [ ] 实现学习系统
- [ ] 多模型集成
- [ ] 生产部署

---

## 🚀 下一步

1. **完成初级改进**: 选择 1-2 个初级改进实践
2. **贡献代码**: 向项目提交 PR
3. **发表文章**: 分享你的学习心得
4. **开发应用**: 基于项目构建实际应用

---

## 📞 获取帮助

- **GitHub Issues**: 报告 Bug 或提问
- **社区讨论**: 微信群交流
- **邮件联系**: 查看项目 README

---

**祝你学习顺利，成为多模态 Agent 专家！🎉**


